import React, { FC } from 'react';
import MainPage from '../../pages/main/MainPage';

const MainLayout: FC = () => {
    return (
        <div>
            Main layout
            <MainPage />
        </div>
    );
};

export default MainLayout;